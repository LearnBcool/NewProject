{
  "compilerOptions": {
    "target": "esnext",                      // Define o padrão de JavaScript gerado, "esnext" usa recursos modernos.
    "lib": ["dom", "dom.iterable", "esnext"], // Permite o uso das APIs do DOM e JavaScript moderno.
    "jsx": "react-jsx",                       // Habilita JSX e usa o novo transformador JSX do React.
    "module": "esnext",                       // Define o sistema de módulos, "esnext" usa import/export.
    "moduleResolution": "node",               // Usa resolução de módulo no estilo Node.js.
    "strict": true,                           // Habilita o modo estrito, que é recomendado para detectar erros.
    "allowSyntheticDefaultImports": true,     // Permite imports de módulos que não possuem exportação padrão.
    "esModuleInterop": true,                  // Permite interoperabilidade com CommonJS e ES Modules.
    "skipLibCheck": true,                     // Ignora checagem de tipos nas bibliotecas, reduzindo o tempo de compilação.
    "forceConsistentCasingInFileNames": true, // Garante que o uso de letras maiúsculas e minúsculas seja consistente em nomes de arquivos.
    "resolveJsonModule": true,                // Permite importar arquivos JSON como módulos.
    "baseUrl": ".",                           // Define o diretório base para resolução de módulos.
    "paths": {
      "@components/*": ["src/components/*"],  // Alias para facilitar a importação de módulos.
      "@assets/*": ["src/assets/*"]
    },
    "noEmit": true                            // Evita gerar arquivos `.js` na saída.
  },
  "include": ["src", "types"],                // Inclui o diretório "src" e "types" para a compilação.
  "exclude": ["node_modules"]                 // Exclui o diretório "node_modules" da compilação.
}